# Optional. Not required — terrain (AWS) and imagery (ESRI) are both keyless.
# Only needed if you switch TERRAIN_SOURCE/SATELLITE_SOURCE to "mapbox"
# in src/terrain/fetcher.py.
MAPBOX_API_KEY=your_mapbox_public_token_here
